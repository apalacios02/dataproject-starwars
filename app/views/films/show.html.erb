<h1><%= @film['title'] %></h1>
<p>Director: <%= @film['director'] %></p>
<p>Producer: <%= @film['producer'] %></p>
<p>Release Date: <%= @film['release_date'] %></p>
<p>Opening Crawl: <%= @film['opening_crawl'] %></p>

<h2>People</h2>
<% if @people.any? %>
  <ul>
    <% @people.each do |person| %>
      <li><%= link_to person['name'], person_path(person['url'].split('/').last) %></li>
    <% end %>
  </ul>
<% else %>
  <p>No people available.</p>
<% end %>

<h2>Vehicles</h2>
<% if @vehicles.any? %>
  <ul>
    <% @vehicles.each do |vehicle| %>
      <li><%= link_to vehicle['name'], vehicle_path(vehicle['url'].split('/').last) %></li>
    <% end %>
  </ul>
<% else %>
  <p>No vehicles available.</p>
<% end %>

<h2>Starships</h2>
<% if @starships.any? %>
  <ul>
    <% @starships.each do |starship| %>
      <li><%= link_to starship['name'], starship_path(starship['url'].split('/').last) %></li>
    <% end %>
  </ul>
<% else %>
  <p>No starships available.</p>
<% end %>

<h2>Planets</h2>
<% if @planets.any? %>
  <ul>
    <% @planets.each do |planet| %>
      <li><%= link_to planet['name'], planet_path(planet['url'].split('/').last) %></li>
    <% end %>
  </ul>
<% else %>
  <p>No planets available.</p>
<% end %>

<%= link_to 'Back to Films', films_path %> |
<%= link_to 'Back to Home', root_path %>
