<h1>Vehicles</h1>
<ul>
  <% @vehicles['results'].each do |vehicle| %>
    <li>
      <%= link_to vehicle['name'], vehicle_path(vehicle['url'].split('/').last) %>
    </li>
  <% end %>
</ul>

<%= link_to 'Back to Home', root_path %>