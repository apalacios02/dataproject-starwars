<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= @person['name'] %></title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-900">

  <div class="container mx-auto p-6">
    <header class="mb-8">
      <h1 class="text-3xl font-bold"><%= @person['name'] %></h1>
    </header>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <section class="mb-6">
        <h2 class="text-xl font-semibold">Details</h2>
        <p><strong>Height:</strong> <%= @person['height'] %></p>
        <p><strong>Mass:</strong> <%= @person['mass'] %></p>
        <p><strong>Hair Color:</strong> <%= @person['hair_color'] %></p>
        <p><strong>Skin Color:</strong> <%= @person['skin_color'] %></p>
        <p><strong>Eye Color:</strong> <%= @person['eye_color'] %></p>
        <p><strong>Birth Year:</strong> <%= @person['birth_year'] %></p>
        <p><strong>Gender:</strong> <%= @person['gender'] %></p>
        <p><strong>Jedi:</strong> <%= @is_jedi ? 'Yes' : 'No' %></p>
        <p><strong>Sith:</strong> <%= @is_sith ? 'Yes' : 'No' %></p>
      </section>

      <section class="mb-6">
        <h2 class="text-xl font-semibold">Homeworld</h2>
        <p><%= link_to @homeworld['name'], planet_path(@homeworld['url'].split('/').last), class: "text-blue-500 hover:underline" %></p>

        <h2 class="text-xl font-semibold mt-6">Films</h2>
        <ul class="list-disc list-inside">
          <% @films.each do |film| %>
            <li><%= link_to film['title'], film_path(film['url'].split('/').last), class: "text-blue-500 hover:underline" %></li>
          <% end %>
        </ul>

        <h2 class="text-xl font-semibold mt-6">Vehicles</h2>
        <ul class="list-disc list-inside">
          <% @vehicles.each do |vehicle| %>
            <li><%= link_to vehicle['name'], vehicle_path(vehicle['url'].split('/').last), class: "text-blue-500 hover:underline" %></li>
          <% end %>
        </ul>

        <h2 class="text-xl font-semibold mt-6">Starships</h2>
        <ul class="list-disc list-inside">
          <% @starships.each do |starship| %>
            <li><%= link_to starship['name'], starship_path(starship['url'].split('/').last), class: "text-blue-500 hover:underline" %></li>
          <% end %>
        </ul>
      </section>
    </div>

    <footer class="mt-8">
      <%= link_to 'Back to People', people_path, class: "text-blue-500 hover:underline" %> |
      <%= link_to 'Back to Home', root_path, class: "text-blue-500 hover:underline" %>
    </footer>
  </div>

</body>
</html>
