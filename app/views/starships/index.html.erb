<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Starships</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-900">

  <div class="container mx-auto p-6">
    <h1 class="text-3xl font-bold mb-6">Starships</h1>

    <ul class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
      <% @starships['results'].each do |starship| %>
        <li class="bg-white rounded-lg shadow-md p-6">
          <h2 class="text-xl font-semibold"><%= starship['name'] %></h2>
          <p>Model: <%= starship['model'] %></p>
          <p>Manufacturer: <%= starship['manufacturer'] %></p>
          <div class="mt-4">
            <%= link_to 'View Details', starship_path(starship['url'].split('/').last), class: "text-blue-500 hover:underline" %>
          </div>
        </li>
      <% end %>
    </ul>

    <div class="mt-8">
      <%= link_to 'Back to Home', root_path, class: "text-blue-500 hover:underline" %>
    </div>
  </div>

</body>
</html>
