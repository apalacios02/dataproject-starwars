<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= @starship['name'] %></title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-900">

  <div class="container mx-auto p-6">
    <h1 class="text-3xl font-bold"><%= @starship['name'] %></h1>
    <p>Model: <%= @starship['model'] %></p>
    <p>Manufacturer: <%= @starship['manufacturer'] %></p>
    <p>Starship Class: <%= @starship['starship_class'] %></p>
    <p>Cost in Credits: <%= @starship['cost_in_credits'] %></p>
    <p>Length: <%= @starship['length'] %></p>

    <h2 class="text-xl font-semibold mt-6">Pilots</h2>
    <% if @pilots.any? %>
      <ul class="list-disc list-inside">
        <% @pilots.each do |pilot| %>
          <li><%= link_to pilot['name'], person_path(pilot['url'].split('/').last), class: "text-blue-500 hover:underline" %></li>
        <% end %>
      </ul>
    <% else %>
      <p>No pilots available.</p>
    <% end %>

    <h2 class="text-xl font-semibold mt-6">Films</h2>
    <% if @films.any? %>
      <ul class="list-disc list-inside">
        <% @films.each do |film| %>
          <li><%= link_to film['title'], film_path(film['url'].split('/').last), class: "text-blue-500 hover:underline" %></li>
        <% end %>
      </ul>
    <% else %>
      <p>No films available.</p>
    <% end %>

    <div class="mt-8">
      <%= link_to 'Back to Starships', starships_path, class: "text-blue-500 hover:underline" %> |
      <%= link_to 'Back to Home', root_path, class: "text-blue-500 hover:underline" %>
    </div>
  </div>

</body>
</html>
